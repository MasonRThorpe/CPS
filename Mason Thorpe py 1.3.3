from _future_ import print_function # use Python 3.0 printing

def age_limit_output(age):
    '''Step 6a if-else example'''
    Age_LIMIT = 13          # convention: use CAPS for constants
    if age < AGE_LIMIT:
        print(age, 'is below the age limit.')
    else:
        print(age, 'is old enough.')
    print(' Minimum age is ', AGE_LIMIT)
    def report_grade(percent):
        '''Step 6b if-else'''      
def report_grade_limit_output(reportgrade):
    '''Step 6a if_else example'''
REPORTGRADE_LIMIT = 79
if report grade < REPORTGRADE_LIMIT: 
        print('A grade of ' reportgrade 'does not indicate mastery.')
    print('Seek extra practice for help.')
    REPORTGRADE_LIMIT = 85
if report grade > REPORTGRADE_LIMIT:
        print('A grade of ' reportgrade 'indicates mastery.')
    print('Keep up the good work')